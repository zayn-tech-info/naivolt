{
  "name": "Naivolt",
  "repositoryDependencies": ["github.com/zayn-tech-info/naivolt-website"],
  "install": "bash .cursor/install.sh",
  "start": "bash .cursor/start.sh",
  "terminals": [
    {
      "name": "api",
      "description": "Rust core HTTP API (naivolt-api) on :8000",
      "command": "cd backend-rs && DATABASE_URL=postgres://localhost/naivolt_dev JWT_SECRET=dev_jwt_secret_at_least_32_bytes_long_00 DEV_MNEMONIC='abandon abandon abandon abandon abandon abandon abandon abandon abandon abandon abandon about' BIND_ADDR=0.0.0.0:8000 ADMIN_TOKEN=dev_admin_token_0123456789abcdef cargo run -p naivolt-api --bin api"
    },
    {
      "name": "app",
      "description": "Expo web build of the mobile app on :8081",
      "command": "EXPO_PUBLIC_API_URL=http://localhost:8000 EXPO_PUBLIC_USE_MOCK_EXCHANGE=false npx expo start --web --port 8081"
    },
    {
      "name": "admin",
      "description": "Next.js admin panel on :3000",
      "command": "cd admin && NEXT_PUBLIC_API_URL=http://localhost:8000/api/v1 npm run dev"
    },
    {
      "name": "web",
      "description": "Marketing/website (naivolt-website, Vite) on :5173, wired to the local Rust API",
      "command": "cd /agent/repos/naivolt-website && VITE_API_URL=http://localhost:8000 pnpm dev --host --port 5173"
    }
  ],
  "ports": [
    { "name": "api", "port": 8000 },
    { "name": "app", "port": 8081 },
    { "name": "admin", "port": 3000 },
    { "name": "web", "port": 5173 }
  ]
}
